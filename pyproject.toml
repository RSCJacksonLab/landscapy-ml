[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "landscapy-ml"
version = "0.1.0"
description = "PyTorch adapters and example training pipelines for Landscapy fitness landscapes"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
license-files = ["LICENSE"]
authors = [
  { name = "Matthew A. Spence", email = "matthew.spence@anu.edu.au" },
]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Science/Research",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Scientific/Engineering :: Bio-Informatics",
]
dependencies = [
  "click>=8.1",
  "landscapy>=0.9,<1",
  "networkx>=3.2",
  "numpy>=1.24",
  "pandas>=2.0",
  "pytorch-lightning>=2.3",
  "scipy>=1.10",
  "tensorboard>=2.20",
  "torch>=2.2",
]

[project.optional-dependencies]
dev = [
  "build>=1.2",
  "numpydoc>=1.7",
  "pre-commit==4.3.0",
  "pydocstyle==6.3.0",
  "pytest>=8.0",
  "pytest-cov>=6.0",
  "pytest-mock>=3.14",
  "ruff==0.12.11",
  "twine>=5.0",
]
graph = ["torch-geometric>=2.6"]
gp = ["gpytorch>=1.13"]
examples = [
  "gpytorch>=1.13",
  "torch-geometric>=2.6",
]
tracking = ["wandb>=0.16"]

[project.urls]
Homepage = "https://github.com/RSCJacksonLab/landscapy-ml"
Issues = "https://github.com/RSCJacksonLab/landscapy-ml/issues"
Repository = "https://github.com/RSCJacksonLab/landscapy-ml"

[project.scripts]
landscapy-ml = "landscapyml.__main__:cli"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]

[tool.ruff.format]
docstring-code-format = true
line-ending = "lf"
quote-style = "double"

[tool.pydocstyle]
convention = "numpy"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"
addopts = [
  "--cov=landscapyml",
  "--cov-report=term-missing",
  "--cov-report=xml",
]
filterwarnings = [
    "ignore:numpy.core._multiarray_umath.*:DeprecationWarning",
    "ignore:.*SwigPyPacked.*:DeprecationWarning",
    "ignore:.*SwigPyObject.*:DeprecationWarning",
    "ignore:.*swigvarlink.*:DeprecationWarning",
]

[tool.coverage.run]
branch = true
source = ["landscapyml"]

[tool.coverage.report]
fail_under = 75
precision = 1
show_missing = true
skip_covered = true

[tool.coverage.xml]
output = "coverage.xml"

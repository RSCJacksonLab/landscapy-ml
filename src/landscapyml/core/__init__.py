"""Core landscapy-to-PyTorch abstractions.

Import concrete APIs from their modules (`core.data`, `core.adaptor`,
`core.inference`, `core.model_registry`, and `core.trainer`) to avoid loading
optional ML stacks unnecessarily.
"""

__all__: list[str] = []
